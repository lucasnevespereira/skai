# Claude Pulse — frost theme config

My personal Pulse setup: frost theme, compact layout, EUR, 24h clock, no animation.

## Install

```bash
mkdir -p ~/.config/claude-status
cp config.json ~/.config/claude-status/config.json
```

Pulse also needs hooks in `~/.claude/settings.json`. Run `/pulse` in Claude Code to set them up automatically.
